# 测试用例清单 - Sprint 1

> **项目名称**：AI Research Assistant  
> **版本**：v0.1  

---

## 1. 测试用例概览

| 模块 | 用例数 | 优先级分布 | 状态 |
|------|--------|-----------|------|
| 用户认证 | 8 | 高：5，中：2，低：1 | 已执行 |
| 论文管理 | 7 | 高：4，中：2，低：1 | 已执行 |

---

## 2. 用户认证模块（TC-AUTH-XXX）

### TC-AUTH-001：用户注册成功

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-AUTH-001 |
| **优先级** | 高 |
| **前置条件** | 服务正常运行，用户名未被占用 |
| **测试数据** | username: testuser, email: test@example.com, password: Test123456 |
| **操作步骤** | 1. 调用 POST /api/auth/register <br> 2. 传入用户名、邮箱、密码 |
| **预期结果** | 返回 200，提示"注册成功" |
| **实际结果** | 返回 200，提示"注册成功"|
| **状态** | 通过 |

---

### TC-AUTH-002：重复用户名注册失败

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-AUTH-002 |
| **优先级** | 高 |
| **前置条件** | 用户 testuser 已存在 |
| **测试数据** | username: testuser, email: test2@example.com, password: Test123456 |
| **操作步骤** | 1. 调用 POST /api/auth/register <br> 2. 使用已存在的用户名 |
| **预期结果** | 返回 400，提示"用户名已存在" |
| **实际结果** | 返回 400，提示"用户名已存在"|
| **状态** | 通过 |

---

### TC-AUTH-003：用户登录成功

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-AUTH-003 |
| **优先级** | 高 |
| **前置条件** | 用户 testuser 已注册 |
| **测试数据** | username: testuser, password: Test123456 |
| **操作步骤** | 1. 调用 POST /api/auth/login <br> 2. 传入用户名和密码 |
| **预期结果** | 返回 200，包含 access_token |
| **实际结果** |返回 200，包含 access_token |
| **状态** | 通过 |

---

### TC-AUTH-004：错误密码登录失败

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-AUTH-004 |
| **优先级** | 高 |
| **前置条件** | 用户 testuser 已注册 |
| **测试数据** | username: testuser, password: WrongPassword |
| **操作步骤** | 1. 调用 POST /api/auth/login <br> 2. 传入错误的密码 |
| **预期结果** | 返回 401，提示"用户名或密码错误" |
| **实际结果** | 返回 401，提示"用户名或密码错误"|
| **状态** |通过|

---

### TC-AUTH-005：不存在的用户登录失败

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-AUTH-005 |
| **优先级** | 中 |
| **前置条件** | 无 |
| **测试数据** | username: nonexistent, password: Test123456 |
| **操作步骤** | 1. 调用 POST /api/auth/login <br> 2. 传入不存在的用户名 |
| **预期结果** | 返回 401，提示"用户名或密码错误" |
| **实际结果** | 返回 401，提示"用户名或密码错误" |
| **状态** | 通过|

---

### TC-AUTH-006：注册时邮箱格式错误

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-AUTH-006 |
| **优先级** | 中 |
| **前置条件** | 服务正常运行 |
| **测试数据** | username: testuser, email: invalid-email, password: Test123456 |
| **操作步骤** | 1. 调用 POST /api/auth/register <br> 2. 传入格式错误的邮箱 |
| **预期结果** | 返回 422，提示邮箱格式错误 |
| **实际结果** | 返回 422，提示邮箱格式错误|
| **状态** | 通过 |

---

### TC-AUTH-007：注册时密码长度不足

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-AUTH-007 |
| **优先级** | 中 |
| **前置条件** | 服务正常运行 |
| **测试数据** | username: testuser, email: test@example.com, password: 123 |
| **操作步骤** | 1. 调用 POST /api/auth/register <br> 2. 传入长度不足的密码（<6位） |
| **预期结果** | 返回 422，提示密码长度至少为6位 |
| **实际结果** | 返回 422，提示密码长度至少为6位|
| **状态** | 通过|

---

### TC-AUTH-008：Token 过期验证

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-AUTH-008 |
| **优先级** | 低 |
| **前置条件** | 用户已登录，Token 即将过期 |
| **操作步骤** | 1. 使用过期的 Token 访问受保护接口 |
| **预期结果** | 返回 401，提示 Token 已过期 |
| **实际结果** | 返回 401，提示 Token 已过期|
| **状态** | 通过 |

---

## 3. 论文管理模块（TC-PAPER-XXX）

### TC-PAPER-001：上传 PDF 成功

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-PAPER-001 |
| **优先级** | 高 |
| **前置条件** | 用户已登录，准备一个有效的 PDF 文件 |
| **测试数据** | sample.pdf |
| **操作步骤** | 1. 调用 POST /api/papers/upload <br> 2. 上传 PDF 文件 |
| **预期结果** | 返回 200，包含 paper_id |
| **实际结果** | 返回 200，包含 paper_id|
| **状态** | 通过 |

---

### TC-PAPER-002：获取论文列表

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-PAPER-002 |
| **优先级** | 高 |
| **前置条件** | 用户已登录，已上传至少一篇论文 |
| **测试数据** | 无 |
| **操作步骤** | 调用 GET /api/papers |
| **预期结果** | 返回 200，包含论文列表 |
| **实际结果** | 返回 200，包含论文列表|
| **状态** | 通过 |

---

### TC-PAPER-003：获取单篇论文详情

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-PAPER-003 |
| **优先级** | 高 |
| **前置条件** | 用户已登录，已上传论文 |
| **测试数据** | paper_id |
| **操作步骤** | 调用 GET /api/papers/{paper_id} |
| **预期结果** | 返回 200，包含论文详细信息 |
| **实际结果** | 返回 200，包含论文详细信息|
| **状态** | 通过 |

---

### TC-PAPER-004：删除论文

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-PAPER-004 |
| **优先级** | 高 |
| **前置条件** | 用户已登录，已上传论文 |
| **测试数据** | paper_id |
| **操作步骤** | 调用 DELETE /api/papers/{paper_id} |
| **预期结果** | 返回 200，提示删除成功 |
| **实际结果** | 返回 200，提示删除成功|
| **状态** | 通过 |

---

### TC-PAPER-005：上传非 PDF 文件失败

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-PAPER-005 |
| **优先级** | 中 |
| **前置条件** | 用户已登录 |
| **测试数据** | test.txt |
| **操作步骤** | 1. 调用 POST /api/papers/upload <br> 2. 上传 .txt 文件 |
| **预期结果** | 返回 400，提示"仅支持 PDF 文件" |
| **实际结果** | 返回 400，提示"仅支持 PDF 文件"|
| **状态** | 通过 |

---

### TC-PAPER-006：更新论文标签

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-PAPER-006 |
| **优先级** | 中 |
| **前置条件** | 用户已登录，已上传论文 |
| **测试数据** | paper_id, tags: ["深度学习", "NLP"] |
| **操作步骤** | 1. 调用 PUT /api/papers/{paper_id}/tags |
| **预期结果** | 返回 200，标签更新成功 |
| **实际结果** | 返回 200，标签更新成功|
| **状态** | 通过  |

---

### TC-PAPER-007：更新论文阅读状态

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-PAPER-007 |
| **优先级** | 低 |
| **前置条件** | 用户已登录，已上传论文 |
| **测试数据** | paper_id, status: "已粗读" |
| **操作步骤** | 调用 PUT /api/papers/{paper_id}/status |
| **预期结果** | 返回 200，状态更新成功 |
| **实际结果** | 返回 200，状态更新成功|
| **状态** | 通过 |

## 4.结论
项目基础功能已全部验证通过。前后端服务稳定运行，用户认证和文献管理核心功能均按预期工作。本轮测试未发现 Bug，项目可以进入下一轮测试。

